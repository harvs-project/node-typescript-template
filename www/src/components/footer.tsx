import * as React from "react";

const Footer = () => {
  return (
    <footer>
      <nav>
        <a href="#">Ouvidoria</a>
        <a href="#">Transparência</a>
        <a href="#">SIC</a>
      </nav>
    </footer>
  );
};

export default Footer;

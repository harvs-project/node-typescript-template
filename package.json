{
  "private": true,
  "scripts": {
    "build": "tsc --generateCpuProfile '.ts.cpuprofile'",
    "test": "mocha",
    "start": "nodemon --enable-source-maps --inspect out/main.js",
    "lint": "concurrently 'eslint .' 'prettier --loglevel warn --check .'",
    "hack": "concurrently 'npm:build -- -w' 'npm:start -- -w out/'"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.2",
    "@types/node": "^14.14.36",
    "@typescript-eslint/eslint-plugin": "^4.19.0",
    "@typescript-eslint/parser": "^4.19.0",
    "concurrently": "^6.0.0",
    "eslint": "^7.22.0",
    "mocha": "^8.3.2",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}

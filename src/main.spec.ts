import { describe, it } from 'mocha'
import { strictEqual as equal } from 'assert'

describe('Array', () =>
  describe('#indexOf()', () =>
    describe('when not present', () =>
      it('should return -1', () => equal([1, 2, 3].indexOf(4), -1)))))
